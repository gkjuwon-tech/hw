# Conet Tactile — 하드웨어 빌드 가이드 (공동창업자 전용)

> **읽는 사람**: 공동창업자. 하드웨어를 한 번도 안 만져본 사람.
> **쓴 사람**: 형 (CEO 겸 기획자 겸 SW 총괄).
> **목적**: 너 이거 보고 부품 사고, 발주 넣고, 조립하고, 펌웨어 굽고, 우리 백엔드에 데이터 박히는 거까지 혼자서 다 하게 만드는 거.
> **톤**: 친절은 하지만 약은 올리는 톤. 빡칠 때마다 한 번씩 비웃을 거임. 익숙해져.
>
> ---
>
> 이 문서랑 별개로 [`HOW_IT_WORKS.md`](./HOW_IT_WORKS.md) 는 "이게 도대체 뭐냐" 를 누구나 이해할 수 있게 설명하는 문서고, [`BOM.md`](./BOM.md) 는 양산 단가 기준 BOM 임. 이 문서 (`HARDWARE_BUILD_GUIDE.md`) 는 "**너 진짜 한 대 만들어** 짜식아" 용임. 그래서 단가는 좀 비싸도 (= 소량 1~10개 기준) 진짜로 구할 수 있는 링크 위주임.

---

## 0. 시작하기 전에 — 너 지금 뭐하는 건지 알고 시작해라

### 0.1 우리 회사 30초 요약

```
공장 컨베이어 벨트 위에 깔판 하나 깔면,
그 위를 지나가는 부품을 손바닥으로 만져보듯 검사해서
불량을 AI 가 자동으로 걸러주는 시스템.

깔판 = 우리가 파는 하드웨어 (1회성)
AI = 우리가 파는 클라우드 (월 구독)
설치 = 가위로 잘라서 양면테이프로 붙임. 끝.
```

이게 전부. 더 자세한 건 [HOW_IT_WORKS.md](./HOW_IT_WORKS.md) 가서 읽어. 안 읽고 부품부터 사려고 하지 마. 안 읽고 사면 너 뭐 사는지도 모르는 채로 30만원 카드 긁는 거야. 진심.

### 0.2 네가 만들 건 이거다 — "프로토타입 1대"

크게 4 덩어리:

```
┌─────────────────────────────────────────────────────────────────────────┐
│ 1) Tactile Mesh    얇은 센서 깔판 (200 × 200 mm 정사각).                │
│    └─ 압력 누르면 저항이 바뀌는 검정 필름 + 그 위에 전도성 천 격자.     │
│                                                                         │
│ 2) Scanner board   ESP32-S3 + 멀티플렉서 2개 + ADC.                      │
│    └─ 깔판의 16×16 = 256 셀을 1초에 200번씩 훑어서 PC 로 쏴줌.          │
│                                                                         │
│ 3) Edge (옵션, 나중)  Jetson Orin Nano 박스. 처음엔 그냥 노트북으로     │
│    돌려도 됨. 진짜 공장 들고 갈 때만 사면 됨.                            │
│                                                                         │
│ 4) Cloud            우리 백엔드. 이 레포의 `backend/`. 형이 다 짰음.    │
│    └─ 너는 그냥 `docker compose up` 한 줄만 치면 됨.                    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 0.3 예산 / 시간

| 항목 | 비용 (1대) | 시간 |
|------|-----------:|------|
| 부품 (Mesh + Scanner + 도구) | **약 20 ~ 25만원** | 발주 + 배송 1~2주 |
| 도구 (인두/멀티미터/커터) — 1회만 | **약 15만원** | 같이 배송 |
| 조립 | 0 (이미 산 거 씀) | **반나절 ~ 하루** |
| 펌웨어 굽기 | 0 | **1시간** |
| 백엔드 띄우기 | 0 | **10분** |
| 첫 inspect 성공 | 0 | **30분** |
| **합계** | **약 35 ~ 40만원** | **약 2주 (대부분 배송 대기)** |

> Edge (Jetson) 빼고 노트북으로 데모하면 위 합계. Jetson 까지 사면 +70만원, 시간은 동일.

### 0.4 마음의 준비

- 1번에 안 됨. 100% 안 됨. 나도 안 됐음. 디버깅이 절반임.
- 인두에 손 데지 마라. 진짜로. 응급실 가지 마.
- 멀티미터는 검정 = 마이너스, 빨강 = 플러스. 헷갈리지 마.
- 부품 도착하고 박스 까자마자 다 잃어버리지 마라. 작은 거 진짜 잘 굴러간다.
- 막히면 형한테 톡해. 단, 사진 찍어서 보내. **"안 됨"** 만 보내지 마.

---

## 1. 하드웨어 vs 소프트웨어 — 너 어디까지 책임지냐

```
                     ┌─────────────────────────────┐
                     │   고객 공장 컨베이어 벨트   │
                     └──────────────┬──────────────┘
                                    │
                  ─── HARDWARE ─────┴──── (← 네 일)
                                    │
              ┌─── Tactile Mesh ────┘
              │   (압력 필름 + 격자 + TPU 코팅)
              │
              ▼
       Scanner board
       (ESP32-S3 + MUX × 2 + ADC)
              │
              │  USB-C
              ▼
   ─── SOFTWARE ─────────────────────────── (← 형 일)
              │
       Edge (Jetson)  ←── Python 추론 데몬
              │
              │  HTTPS
              ▼
       Tactile Cloud  ←── FastAPI 백엔드 (이 레포 `backend/`)
              │
              ▼
       대시보드 / 슬랙 / MES
```

**네 일 (조립 + 발주 + 배송)** = 위 그림에서 `HARDWARE` 박스 안의 모든 것 + 케이스 + 케이블.
**형 일 (소프트웨어)** = `SOFTWARE` 박스 안의 모든 것.
**둘 다 책임지는 거** = USB-C 케이블 빠지면 너랑 나랑 둘 다 망함.

---

## 2. 부품 쇼핑 리스트 — **이 표 그대로 카드 긁으면 됨**

> ⚠️ **가격은 2026년 5월 기준 환율 (1 USD ≈ ₩1,360, 1 USD ≈ 168 ¥, 1 USD ≈ 7.3 ¥) 으로 환산한 거고, 배송비/관세 별도임.** 시간 지나면 살짝 바뀌니까 결제 화면 가격을 진짜 가격이라고 생각해라.
>
> ⚠️ **링크는 끊어질 수 있음.** 끊어졌으면 **부품명을 그대로 복사해서** 같은 사이트 검색창에 붙여넣으면 100% 나옴. "링크 안 됨" 이라고 보내지 마. 그냥 검색해.
>
> 💡 한국에서 발주: **디바이스마트** 가 가장 빠름 (다음날 도착). 해외 부품은 **Digi-Key** (3~5일, 관세 있음) 또는 **Adafruit** (5~10일, 관세 있음). 알리는 싸지만 2~4주.

### 2.1 깔판 (Tactile Mesh) — **약 8만원**

| # | 부품 | 사야할 수량 | 어디서 사? | 단가 (대략) | 링크 / 검색어 |
|---|------|------------|-----------|------------:|--------------|
| M1 | **Velostat / Linqstat 시트** (압력 감지 필름, A4 크기 1장) | 1장 (A4 / 280×280 mm) | Adafruit | $4.95 ≈ ₩6,800 | [Adafruit #1361](https://www.adafruit.com/product/1361) — 검색어: `Velostat 1361` |
| M2 | **전도성 천** (silver-plated nylon ripstop, 50 mΩ/sq, A4 크기 정도) | 1장 (A4 ~ A3) | Adafruit | $19.95 ≈ ₩27,000 | [Adafruit #1167](https://www.adafruit.com/product/1167) — 검색어: `conductive fabric ripstop` |
| M3 | **전도성 실** (silver-plated 4-ply) | 1롤 | Adafruit | $6.95 ≈ ₩9,500 | [Adafruit #641](https://www.adafruit.com/product/641) — 검색어: `conductive thread 641` |
| M4 | **Kapton 테이프** (25 µm × 12 mm 폭 × 33 m) | 1롤 | 디바이스마트 / 쿠팡 / 알리 | ₩6,000 ~ 12,000 | 디바이스마트 검색: `Kapton tape 12mm` |
| M5 | **TPU 라미네이팅 필름** (식품등급, A4 한 묶음) | A4 10장 묶음 1팩 | 쿠팡 / 알리 | ₩8,000 ~ 15,000 | 쿠팡 검색: `라미네이팅 필름 A4 무광` (광택 NO, 무광 / 매트 위주) |
| M6 | **3M VHB 5952 양면테이프** (25 mm × 33 m) | 1롤 | 쿠팡 / 그레인저 | ₩28,000 | 쿠팡 검색: `3M VHB 5952` |
| M7 | **FFC 리본 케이블** (16-pin, 1.0 mm pitch, 200 mm) ⚠️ 우리 프로토는 ESP32 직접 점퍼와이어 연결하니까 처음엔 **안 사도 됨**. PCB 만들 때 사라. | 0 | — | — | — |

**소계: 약 8만원.** Adafruit 3개 (M1, M2, M3) 같이 시키면 배송비 절약됨.

> 💡 **알리 대체**: Velostat (= "EeonTex anti-static film", "carbon black polyethylene film") 알리에서 1m × 1m 한 장이 ₩15,000 정도. 처음에는 Adafruit 가 안전. 알리에서 사면 두께/저항 편차 큼.

### 2.2 스캐너 보드 (브레드보드 버전) — **약 5만원**

> PCB 까지 만들면 본격적이지만 처음엔 **브레드보드 + 점퍼와이어** 로 충분함. PCB 는 동작 확인 후 만드는 거.

| # | 부품 | 수량 | 어디서 사? | 단가 | 링크 / 검색어 |
|---|------|------|-----------|-----:|--------------|
| S1 | **ESP32-S3-DevKitC-1** (Espressif 공식 개발보드, N8R8 = 8MB flash + 8MB PSRAM) | 1 | 디바이스마트 | ₩18,000 | 디바이스마트 검색: `ESP32-S3-DevKitC-1-N8R8` |
| S2 | **CD74HC4067 16채널 아날로그 멀티플렉서 모듈** | **2개** (행 1개 + 열 1개) | 디바이스마트 / 알리 | ₩4,000 / 개 = ₩8,000 | 디바이스마트 검색: `CD74HC4067 모듈` 또는 SparkFun BOB-09056 |
| S3 | **ADS1115 16-bit ADC 모듈** (I²C, 4채널, PGA) | 1 | 디바이스마트 / 쿠팡 | ₩4,500 | 디바이스마트 검색: `ADS1115 모듈` 또는 [Adafruit #1085](https://www.adafruit.com/product/1085) |
| S4 | **브레드보드** (830홀, 일반형) | 1 | 쿠팡 / 디바이스마트 | ₩3,000 | 쿠팡 검색: `브레드보드 830` |
| S5 | **점퍼 와이어** (수-수, 수-암, 암-암 각 40개씩 키트) | 1세트 | 쿠팡 | ₩4,000 | 쿠팡 검색: `점퍼와이어 키트 듀폰` |
| S6 | **USB-C 데이터 케이블** (50 cm ~ 1 m, **데이터 가능한 거**. "충전 전용" 안 됨) | 1 | 아무 데나 | ₩5,000 ~ 8,000 | 쿠팡 검색: `USB-C 데이터 케이블 충전 + 데이터` (꼭 데이터 지원 명시된 거) |
| S7 | **풀업 저항 10 kΩ × 4개** + **0.1 µF 세라믹 캡 × 4개** (디커플링) | 키트 | 쿠팡 / 디바이스마트 | ₩2,000 (저항/캡 키트로 사면 더 쌈) | 쿠팡 검색: `저항 1/4W 키트` |

**소계: 약 5만원.**

> 💡 **돈 아끼고 싶으면 (= 알리 OK 인 경우)**: ESP32-S3 알리 ₩9,000, MUX 모듈 알리 ₩1,500 / 개. 합계 ₩2만원대로 떨어짐. 단 배송 2~4주. 시간 지나도 동작은 똑같음. 우리는 일단 빠르게 동작하는 게 우선이라 디바이스마트 추천.

### 2.3 도구 (한 번만 사면 됨) — **약 15만원**

이건 평생 쓸 거니까 좀 좋은 거 사라.

| # | 도구 | 수량 | 어디서 사? | 단가 | 링크 / 검색어 |
|---|------|------|-----------|-----:|--------------|
| T1 | **납땜 인두 + 거치대 + 솔더** 세트 (60W 온도조절형, 일반 키트면 됨) | 1세트 | 쿠팡 / 디바이스마트 | ₩30,000 ~ 50,000 | 쿠팡 검색: `납땜 인두 키트 온도조절`. 좀 좋은 거 원하면 **Pinecil V2** (₩90,000, [pine64.com](https://pine64.com/product/pinecil-smart-mini-portable-soldering-iron/)) |
| T2 | **솔더링 와이어** (Sn60/Pb40 또는 무연, 0.5 mm 100g) | 1롤 | 쿠팡 | ₩8,000 | 쿠팡 검색: `솔더 0.5mm` |
| T3 | **디지털 멀티미터** (Fluke 안 사도 됨. 한 1만원짜리면 충분) | 1 | 쿠팡 | ₩15,000 | 쿠팡 검색: `디지털 멀티미터 DT-830B` |
| T4 | **정밀 가위** (전기공사용) | 1 | 쿠팡 | ₩5,000 | 쿠팡 검색: `전선 가위 정밀` |
| T5 | **로타리 커터** (45 mm + 자) 깔판 자를 때 씀 | 1 | 쿠팡 | ₩10,000 | 쿠팡 검색: `로타리 커터 45mm` |
| T6 | **모눈 자** (300 mm 스테인리스, 미끄럼 방지) | 1 | 쿠팡 | ₩6,000 | 쿠팡 검색: `스테인리스 자 300mm` |
| T7 | **A3 라미네이팅 머신** (가정용 ₩50,000 ~ 80,000) | 1 | 쿠팡 | ₩60,000 | 쿠팡 검색: `라미네이팅 머신 A3 가정용` |
| T8 | **방진 매트** (또는 그냥 박스 종이) | 1 | 쿠팡 | ₩3,000 | 쿠팡 검색: `정전기방지 작업매트` |

**소계: 약 14 ~ 15만원.**

> 💡 **이미 있는 거 빼라.** 인두/멀티미터 같은 거 회사 어딘가에 굴러다닐 수도 있음. 형도 어디 박혀있을지 몰라. 일단 책상 서랍 다 까봐.

### 2.4 Edge 박스 (나중에 사) — **약 75만원**

> ⚠️ **처음 1대 만들 때는 사지 마.** Edge 박스의 역할은 "현장에 들고 가서 24/7 추론 돌리는 거" 인데, 데모하고 동작 확인하는 단계에선 **그냥 너 노트북** 으로 충분함. Cloud 백엔드를 노트북에서 띄우고 거기에 USB 로 스캐너 연결하면 끝.
>
> 진짜 첫 고객 라인에 들어갈 때 사면 됨.

| # | 부품 | 어디서 | 단가 | 링크 |
|---|------|--------|-----:|------|
| E1 | **NVIDIA Jetson Orin Nano 8GB Developer Kit** (Part #945-13766-0000-000) | NVIDIA 공식 / Arrow / Seeed Studio / 한국 총판 (엔디비아 코리아 통한) | $499 ≈ ₩680,000 | [seeedstudio.com 검색](https://www.seeedstudio.com) `Jetson Orin Nano Developer Kit` |
| E2 | **NVMe M.2 SSD 256GB** (Western Digital SN570 등) | 쿠팡 / 다나와 | ₩40,000 | 쿠팡 검색: `WD SN570 256GB` |
| E3 | **PoE+ 스플리터 (24V DC out)** (TP-Link TL-POE10R 등) | 쿠팡 | ₩35,000 | 쿠팡 검색: `TP-Link PoE 스플리터 TL-POE10R` |
| E4 | **알루미늄 인클로저** (Hammond 1554 시리즈 또는 다이캐스트 박스) | 디지키 / 쿠팡 | ₩30,000 | 쿠팡 검색: `알루미늄 다이캐스트 박스 200x150` |

**소계: 약 75만원.** 첫 1대는 건너뛰어도 됨.

---

## 3. 발주 어떻게 넣냐 — **사이트별 가이드**

> 형은 발주를 너무 자주 해서 자동조종 모드인데, 너는 처음일 수 있음. 그래서 사이트별로 풀어줌. 이미 아는 데는 건너뛰어라.

### 3.1 디바이스마트 (devicemart.co.kr) — 한국, 다음날 도착

1. [devicemart.co.kr](https://www.devicemart.co.kr/) 접속 → **회원가입** (개인 사업자 등록증 있으면 사업자로. 부가세 환급받음.).
2. 우측 상단 검색창에 부품명 그대로 입력. 예: `ESP32-S3-DevKitC-1-N8R8`.
3. 검색 결과 → 상품 클릭 → **장바구니** 담기.
4. 위의 표 (`2.2`) 의 모든 디바이스마트 항목 다 담기.
5. 장바구니 → **주문하기** → 배송지 입력 → **무통장입금** 또는 **신용카드** 결제.
6. **다음 영업일 오후** 에 도착함. 단, **오후 3시 이전에 결제해야** 당일 출고.
7. 영수증/세금계산서 발급 가능. 사업자등록증으로 회원가입하면 자동.

> 💡 디바이스마트는 **재고 없으면 "예약주문"** 으로 표시됨. 예약은 1~3주 걸리니까 가능하면 "재고있음" 만 카트에 담아.

### 3.2 쿠팡 — 한국, 로켓배송이면 다음날

1. 그냥 쿠팡 검색해서 사면 됨. 모르겠으면 너 부모님한테 물어봐.
2. **로켓배송** 마크 있는 것만 골라. 일반 판매자는 배송 1주일 걸림.
3. 결제. 끝. 너 카드로 회사 카드로 긁어. (지출증빙 챙겨. 영수증 안 잃어버리게.)

### 3.3 Adafruit (adafruit.com) — 미국, 5~10일

1. [adafruit.com](https://www.adafruit.com/) → 회원가입 (이메일만 있으면 됨).
2. 상단 검색창에 부품 번호 입력. 예: `1361` 검색 → Velostat 페이지.
3. **Add to Cart**.
4. 위의 표 (`2.1`) 의 Adafruit 항목 다 담기 (M1, M2, M3).
5. 장바구니 우측 상단 → **Checkout**.
6. 배송지: 한국 주소 영문으로. 예:
   ```
   Name: Gildong Hong
   Address Line 1: 123-45 Teheran-ro, Gangnam-gu
   Address Line 2: 7F Conet Studio
   City: Seoul
   State/Province: Seoul
   Postal Code: 06234
   Country: South Korea
   Phone: +82-10-1234-5678
   ```
7. 배송 옵션: **USPS Priority Mail International** ($35 정도) 또는 **DHL Express** ($55 정도, 더 빠름). DHL 추천.
8. 결제: 신용카드 (VISA / Master).
9. **관세**: 한국 도착 시 부가세 10% + 관세 8% 부과될 수 있음. **개인 사용 합산 가격 $150 이하면 면세**. 위 표 (`2.1`) 합산이 $35 이라서 면세권에 들어갈 가능성 높음. 단, 다른 부품이랑 같이 사면 합산되니까 주의.
10. 배송 추적은 Adafruit 이메일에 들어옴.

> 💡 **개인통관고유부호** 필요함. 모르면 [관세청 홈페이지](https://unipass.customs.go.kr) 가서 1분 만에 발급. 안 받으면 통관에서 막힘.

### 3.4 Digi-Key (digikey.com / digikey.kr) — 미국, 3~5일

1. [digikey.kr](https://www.digikey.kr) (한국어 사이트) 접속 → 회원가입.
2. 상단 검색창에 부품 번호 또는 디지키 번호 입력. 예: `296-ESP32-S3-DEVKITC-1-N8R8-ND`.
3. 디지키는 진짜 부품이 너무 많아서 검색 결과 필터링 잘 해야 함. **재고 있음** + **국내 배송** 으로 필터.
4. 장바구니 → 결제.
5. 한국 디지키는 **부산 창고** 에 일부 재고가 있어서 빠르면 **2일 만에 도착**.
6. **₩150,000 이상 주문 시 무료 배송**.

> 💡 Digi-Key 가격이 Adafruit 보다 보통 30~50% 쌈. 단 부품이 raw 라서 모듈/브레이크아웃 형태가 아닌 게 많아. 모듈은 디바이스마트가 편함.

### 3.5 알리익스프레스 (ko.aliexpress.com) — 중국, 2~4주

1. 회원가입 → 검색 → 카트 → 결제. 패스워드 절대 다른 사이트랑 같은 거 쓰지 마.
2. **카드 결제** 또는 **카카오페이** 가능.
3. **국내 도착 보장 발송** (보통 7~10일) 옵션 있으면 우선 선택.
4. 알리는 **랜덤 불량** 이 가끔 있음. ESP32 모듈 같은 거 알리 짭이 50% 정도. 발주 전에 셀러 리뷰 100개 이상 + 별 4.7 이상인 데서 사라.

> 💡 형 룰: **신호용 부품 (MCU, ADC, MUX)** 은 디바이스마트/디지키. **수동부품 (저항/캡/케이블/박스)** 은 알리 또는 쿠팡.

### 3.6 발주 체크리스트 — 카드 긁기 전에 5초만

- [ ] 위의 표 (`2.1`, `2.2`, `2.3`) 의 모든 항목을 카트에 담았다.
- [ ] 같은 사이트 항목은 한 번에 결제 (배송비 절약).
- [ ] 한국 배송지 입력 시 영문 + 국가코드.
- [ ] 해외 결제 시 개인통관고유부호 입력란 채움.
- [ ] 카드는 회사 카드 (지출증빙용).
- [ ] 영수증/세금계산서 발급 가능한 사이트에서는 발행 요청.
- [ ] 도착 예정일을 캘린더에 박아라. 형이 "어디까지 됐어?" 물어보면 답할 수 있게.

---

## 4. 조립 — **드디어 손 더러워질 시간**

> ⏱️ **총 소요 시간**: 깔판 1시간 + 스캐너 보드 1시간 + 라미네이팅 30분 = **약 3시간**.
>
> 🩹 **안전**:
> - 인두 온도 350°C ~ 380°C. 손 대지 마라.
> - 환기 시켜라. 솔더 연기 안 좋다.
> - Velostat 가위질할 때 손 베지 마라. 종이 베듯이 베인다.

### 4.1 깔판 만들기 — 16 × 16 = 256 셀

> 깔판의 원리: **압력을 받으면 저항이 떨어지는 검정 필름 (Velostat)** 위·아래에 **전도성 천을 격자로 깐 거**. 가로줄 1개 (행) 과 세로줄 1개 (열) 의 교차점을 측정하면 그 셀의 압력을 알 수 있음. 행 16 × 열 16 = 256 개 셀. ESP32 가 1초에 200번씩 256 셀을 다 훑음.

```
┌─ Top TPU 코팅 ───────────────────────────────────────┐
│ ─── 열 1 ───  ─── 열 2 ───  ───  ...  ─── 열 16 ─── │  ← 세로줄 (전도성 천 16가닥)
├──────────────────────────────────────────────────────┤
│   ░░░░░░░░░░ Velostat 압력 필름 ░░░░░░░░░░░░░░░░░░  │  ← 압력으로 저항이 변하는 검정 필름
├──────────────────────────────────────────────────────┤
│ ─── 행 1 ───────────────────────────────────────────  │
│ ─── 행 2 ───────────────────────────────────────────  │  ← 가로줄 (전도성 천 16가닥)
│   ...                                                │
│ ─── 행 16 ──────────────────────────────────────────  │
├──────────────────────────────────────────────────────┤
│              Bottom TPU 코팅                          │
└──────────────────────────────────────────────────────┘
```

#### 단계별

1. **종이에 격자 그려라**. 200 × 200 mm 정사각. 한 셀 = 12.5 × 12.5 mm. 자 + 연필. 진짜로 그려라.
2. **Velostat 자르기**: 로타리 커터 + 자 → 200 × 200 mm 정사각 한 장 잘라.
   - 💡 Velostat 한 장으로 두 장 만들지 마. 처음엔 한 장에 한 라인으로.
3. **전도성 천을 가로줄로 자르기**: 너비 **5 mm**, 길이 **220 mm** (양쪽 끝 10mm 씩 케이블 연결용 여유). **16 가닥** 필요.
   - 💡 천이 너무 잘 풀어져서 짜증날 거임. 끝단에 투명 매니큐어 발라서 굳히면 안 풀림. (진심이다. 형도 처음에 이거 몰랐음.)
4. **Velostat 한쪽 면에 가로줄 16개 평행하게 깔기**: 줄 간격 **12.5 mm**. Kapton 테이프로 양끝만 임시 고정.
   - 💡 자를 대고 한 줄씩 정렬해라. 비뚤어지면 셀이 사다리꼴 됨.
5. **위에 Velostat 한 장 더** 또는 같은 한 장을 접어 덮기. 압력층이 두 도전층 사이에 끼이는 구조면 됨.
6. **세로줄 16개를 가로줄과 직교** 하게 깔기. 위 단계와 동일.
7. **각 줄 끝단 처리**: 가로줄 16가닥 한쪽 끝, 세로줄 16가닥 한쪽 끝 → **전도성 실** 로 매듭 + 전도성 천 끝을 살짝 노출해서 점퍼와이어 클립을 물릴 수 있게.
   - 💡 양산은 ZIF FFC 커넥터 + 전용 압착기 씀. 우리는 프로토라서 점퍼와이어 클립으로 충분.
8. **TPU 라미네이팅 필름** 위·아래로 한 장씩 덮고 라미네이팅 머신 통과. 끝.
9. **밑면에 3M VHB 양면테이프**: 컨베이어 부착용. 데모할 땐 그냥 책상에 깔아도 됨. **보호지는 떼지 마라** (실제 설치 직전까지).

#### 깔판 동작 확인 — 멀티미터 검사 (5분)

라미네이팅 전에! 한 번 검사하고 라미네이팅 해라. 안 그러면 라미네이팅 후 못 고침.

1. 멀티미터 → 저항 측정 모드 (Ω) → 200kΩ 레인지.
2. 빨강 프로브 → 가로줄 1번. 검정 프로브 → 세로줄 1번.
3. 손가락으로 그 교차점 (셀 1,1) 을 꾹 눌러봐.
   - **누르기 전**: 30 ~ 100 kΩ
   - **누른 후**: 2 ~ 10 kΩ
4. 누를 때 저항이 떨어지면 정상. **안 떨어지면**:
   - 격자가 단선 → 어디서 끊겼는지 확인
   - 위·아래 도전층이 직접 닿음 (단락) → Velostat 한 겹 더 깔거나 절연 필름 추가
5. 256셀 다 검사할 필요는 없음. **랜덤하게 8~10셀** 만 찍어보면 됨.

### 4.2 스캐너 보드 — 브레드보드 + ESP32

> 우리 펌웨어 (`firmware/tactile_scanner_esp32/tactile_scanner_esp32.ino`) 는 ESP32-S3 의 GPIO 핀 번호가 박혀있음. 그 핀 그대로 따라가면 됨.

#### 핀맵 — **이 표대로 점퍼 와이어 꽂아**

```
ESP32-S3-DevKitC-1                CD74HC4067 모듈 (행용)     CD74HC4067 모듈 (열용)
─────────────────────             ─────────────────────       ─────────────────────
GPIO 4  ────────────────────────→ S0                          
GPIO 5  ────────────────────────→ S1                          
GPIO 6  ────────────────────────→ S2                          
GPIO 7  ────────────────────────→ S3                          
                                                              
GPIO 15 ─────────────────────────────────────────────────────→ S0
GPIO 16 ─────────────────────────────────────────────────────→ S1
GPIO 17 ─────────────────────────────────────────────────────→ S2
GPIO 18 ─────────────────────────────────────────────────────→ S3

3V3     ─────────────────────────┬───────────────────────────┬─→ VCC
GND     ─────────────────────────┴───────────────────────────┴─→ GND, EN (둘 다 GND)

행 MUX SIG (공통 단자) ──→ ESP32 의 GPIO 1 (ADC1_CH0) 으로 직결
                            
행 MUX 의 채널 0~15 (Y0~Y15) ──→ 깔판의 가로줄 1~16
열 MUX 의 채널 0~15 (Y0~Y15) ──→ 깔판의 세로줄 1~16

열 MUX SIG (공통 단자) ──→ GND (또는 3V3 절반 분배기 통해서 1.65V 풀-다운, 아래 참고)
```

> 📐 **회로 원리**: ESP32 가 행 MUX 채널 r 과 열 MUX 채널 c 를 선택하면, 그 셀 (r, c) 의 Velostat 저항이 ADC 에 걸림. 단순 분압 회로. 셀 압력 → 저항 ↓ → ADC 값 ↑.

#### 단계별

1. **브레드보드에 ESP32-S3 꽂기**: USB-C 가 한 쪽으로 나오게.
2. **MUX 모듈 2개 브레드보드에 꽂기**: 행용, 열용. 충분히 띄워라 (셀렉트 핀 헷갈리지 않게).
3. **점퍼와이어로 위 핀맵대로 연결**. 한 가닥씩 연결할 때마다 멀티미터로 연결 확인 (저항 모드, 양 끝 잡고 0 Ω 나오면 OK).
4. **깔판의 가로줄 16가닥** → **행 MUX 의 Y0 ~ Y15** 채널에 연결. 점퍼와이어 + 클립.
5. **깔판의 세로줄 16가닥** → **열 MUX 의 Y0 ~ Y15** 채널에 연결.
6. **풀-다운 저항** (선택): 열 MUX SIG (공통) 단자에 **10 kΩ 풀-다운 저항** 을 GND 로. 신호가 떠다니는 거 막아줌.
7. **전원 확인**: ESP32 USB-C 꽂기 전에 점퍼 다시 한 번 검사. **3V3 가 GND 와 직결되어 있으면 ESP32 죽음.**
8. **USB-C 로 노트북 연결.** ESP32 의 빨간 LED 켜지면 살아있는 거임.

> ⏱️ 이 단계 처음이면 1.5~2시간 잡아. 점퍼와이어 헷갈려서 분명 한두 번은 다 뽑고 다시 함.

### 4.3 라미네이팅 (깔판 마무리, 30분)

1. 깔판 위·아래에 **A4 TPU 라미네이팅 필름** 한 장씩 덮음.
2. A3 라미네이팅 머신 켜고 예열 (보통 3~5분).
3. 깔판을 천천히 통과시킴. **빠르면 기포 생김.** 천천히 일정하게.
4. 식힌 후 가장자리 깔끔하게 가위로 다듬음.
5. 끝.

> ⚠️ **라미네이팅 전에 멀티미터 검사 다 끝낸 거 맞지?** 라미네이팅 후 못 고침.

---

## 5. 펌웨어 굽기 — Arduino IDE 로 ESP32-S3 굽는 법

### 5.1 Arduino IDE 설치 (15분)

1. [arduino.cc/en/software](https://www.arduino.cc/en/software) → Windows / macOS 설치 파일 다운로드.
2. 설치. 실행.
3. **File → Preferences → Additional Boards Manager URLs** 에 아래 URL 붙여넣기:
   ```
   https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
   ```
4. **Tools → Board → Boards Manager** → 검색창에 `esp32` → **"esp32 by Espressif Systems"** 설치 (시간 좀 걸림, 1~3분).
5. **Tools → Board → esp32 → "ESP32S3 Dev Module"** 선택.
6. **Tools → USB CDC On Boot → Enabled** ⚠️ 이거 안 켜면 USB 시리얼 안 나옴.
7. **Tools → Port** 에서 ESP32-S3 가 꽂힌 COM 포트 선택 (Windows: `COM3` 등 / macOS: `/dev/tty.usbserial-XXX`).

### 5.2 펌웨어 코드 열기 + 굽기 (10분)

1. 이 레포 클론 받기:
   ```bash
   git clone https://github.com/gkjuwon-tech/hw.git
   cd hw/firmware/tactile_scanner_esp32/
   ```
2. **Arduino IDE 에서 `tactile_scanner_esp32.ino` 열기**.
3. 우측 상단 **체크 버튼 (✓)** = 컴파일. 처음엔 30초~1분 걸림. 에러 없으면 OK.
4. 우측 상단 **→ 버튼** = 업로드. 1분 정도. 끝나면 보드의 빨간 LED 가 깜빡임.

### 5.3 데이터 나오나 확인 — `screen` 또는 `pyserial` (5분)

펌웨어가 200 Hz 로 256 byte + 헤더를 USB CDC 로 토해내고 있음. 이걸 받아 봐야 함.

**macOS / Linux**:
```bash
# 포트 확인
ls /dev/tty.usb*
# 시리얼 모니터 (가공된 텍스트만 보면 됨; 바이너리는 binary로 흘러나옴)
screen /dev/tty.usbserial-XXX 2000000
# 종료: Ctrl+A 누른 후 K → Y
```

**Windows**:
- PuTTY 다운로드 → Serial → Speed `2000000` → Line `COM3` (자기 포트) → Open.

뭐가 잔뜩 흘러내리면 **데이터 나오는 중**. 깔끔한 텍스트가 아니라 외계어가 나옴 — 바이너리라서 정상. 점퍼와이어 다 뽑고 ESP32만 꽂아서 USB로 연결한 상태에서도 데이터 흐름은 있어야 함 (단, 모든 ADC 값이 0 으로 나옴).

### 5.4 Python 으로 프레임 파싱 (10분)

다음 스크립트 저장 후 실행:

```python
# save as: tools/read_frames.py
import serial
import struct
import sys

PORT = "/dev/tty.usbserial-XXX"  # ← 너 포트로 바꿔
BAUD = 2_000_000
HEADER = struct.Struct("<IHHIIHH")  # magic, rows, cols, seq, ts, crc, pad
MAGIC = 0x434F4E54

def read_exact(s, n):
    buf = b""
    while len(buf) < n:
        chunk = s.read(n - len(buf))
        if not chunk:
            sys.exit("port closed")
        buf += chunk
    return buf

with serial.Serial(PORT, BAUD, timeout=1) as s:
    while True:
        # find magic
        b = read_exact(s, 1)
        if b != b"T":   # 'T' is the last byte of 'CONT' little-endian
            continue
        rest_of_header = read_exact(s, HEADER.size - 1)
        full = b + rest_of_header
        magic, rows, cols, seq, ts, crc, _ = HEADER.unpack(full)
        if magic != MAGIC:
            continue
        frame = read_exact(s, rows * cols)
        print(f"seq={seq:6d} rows={rows} cols={cols} avg={sum(frame)/len(frame):.1f}")
```

`pip install pyserial` 한 뒤 `python tools/read_frames.py` 돌리면 1초에 200번 프레임이 찍힘. **avg** 값이 깔판 누를 때 올라가야 함. 안 올라가면 깔판 ↔ MUX 배선 오류.

---

## 6. 백엔드 띄우기 — `docker compose up` 한 줄

### 6.1 사전 준비 (5분)

너 노트북에 아래 둘이 있어야 함:

1. **Docker Desktop** (Windows / macOS): [docker.com/products/docker-desktop/](https://www.docker.com/products/docker-desktop/) 가서 받아.
2. **Git**: 이미 깔려있을 거임. 없으면 [git-scm.com](https://git-scm.com) 가서 받아.

### 6.2 띄우기 (5분)

```bash
git clone https://github.com/gkjuwon-tech/hw.git
cd hw/backend
docker compose up --build
```

빌드 1~3분 걸림. 끝나면:

- 백엔드 API: http://localhost:8000
- API 문서 (Swagger): http://localhost:8000/docs
- 메트릭 (Prometheus): http://localhost:8000/metrics
- 헬스 체크: http://localhost:8000/healthz

### 6.3 첫 호출 — 정상 작동 확인 (1분)

```bash
curl http://localhost:8000/healthz
# → {"status":"ok","version":"0.1.0","environment":"production"}

curl http://localhost:8000/v1/pricing/catalog
# → {"belt_widths":[...], "edge_appliance_krw":1690000, "cloud_tiers":[...]}
```

이게 나오면 백엔드는 끝.

> 💡 더 자세한 API 호출 (라인 만들기 / 프레임 보내기 / 캘리브레이션 / inspect) 은 http://localhost:8000/docs 에서 직접 눌러보면 됨. Swagger UI 가 다 가르쳐줌.

---

## 7. ESP32 ↔ 백엔드 연결 — **첫 inspect 까지**

### 7.1 흐름

```
깔판 → 스캐너 (ESP32) → Python 게이트웨이 스크립트 → HTTP POST → 백엔드 (Docker)
```

ESP32 가 USB 로 바이너리 프레임을 토하고 있고, 백엔드는 HTTP/JSON 만 받음. 둘을 잇는 **게이트웨이 스크립트** 한 개 만들어야 함. 아래.

### 7.2 게이트웨이 스크립트

```python
# save as: tools/gateway.py
import serial
import struct
import requests
import time

PORT = "/dev/tty.usbserial-XXX"  # ← 너 포트
BAUD = 2_000_000
HEADER = struct.Struct("<IHHIIHH")
MAGIC = 0x434F4E54

API = "http://localhost:8000"
LINE_ID = None  # 처음엔 None, 라인 만든 뒤 채움

def post_json(path, payload):
    r = requests.post(f"{API}{path}", json=payload, timeout=5)
    r.raise_for_status()
    return r.json()

def read_frame(s):
    while True:
        b = s.read(1)
        if b != b"T":
            continue
        rest = s.read(HEADER.size - 1)
        magic, rows, cols, seq, ts, crc, _ = HEADER.unpack(b + rest)
        if magic != MAGIC:
            continue
        data = s.read(rows * cols)
        return rows, cols, list(data)

def main():
    global LINE_ID
    # 1) 라인 만들기
    line = post_json("/v1/lines", {"name": "test-line-01", "rows": 16, "cols": 16})
    LINE_ID = line["id"]
    print(f"Created line {LINE_ID}")
    with serial.Serial(PORT, BAUD, timeout=1) as s:
        # 2) 정상품 5개 프레임 모으기
        print("Send 5 normal samples now...")
        for i in range(5):
            rows, cols, frame = read_frame(s)
            post_json(f"/v1/lines/{LINE_ID}/frames", {"frame": frame})
            print(f"  buffered frame {i+1}/5")
            time.sleep(2)  # 사람이 정상품 갈아끼울 시간
        # 3) 캘리브레이션
        result = post_json(f"/v1/lines/{LINE_ID}/calibrate", {})
        print(f"Calibration done: {result}")
        # 4) 본격 inspect 루프
        print("Inspecting forever — Ctrl+C to stop")
        while True:
            rows, cols, frame = read_frame(s)
            verdict = post_json(f"/v1/lines/{LINE_ID}/inspect", {"frame": frame})
            print(f"  verdict={verdict['verdict']} score={verdict['score']:.3f} drift_z={verdict.get('drift_z', 0):.2f}")

if __name__ == "__main__":
    main()
```

### 7.3 실행 (3분)

```bash
cd hw
pip install pyserial requests
python tools/gateway.py
```

그러면:
1. 라인 자동 생성됨.
2. 너한테 "정상품 5개 프레임 보내라" 라고 떠. 깔판 위에 **정상품 (예: 빵 1개, 또는 무거운 책)** 5개를 차례로 올렸다 내렸다 해.
3. 캘리브레이션 자동 실행.
4. 그 뒤로는 **inspect 루프**. 깔판 위에 뭐 올릴 때마다 `verdict` 가 `pass` 또는 `fail` 로 떨어짐.
5. **정상품을 올리면 `pass`**. **다른 무게/모양의 물건을 올리면 `fail`** 떠야 함.

이 시점에서 너 **데모 가능한 상태** 임. 형한테 영상 찍어 보내라. 자랑할 때임.

> 💡 백엔드의 Swagger (http://localhost:8000/docs) 에서 `/v1/lines/{id}/events` 를 EventSource 로 열면 inspect 결과가 실시간 SSE 로 흘러내림. 대시보드 만들 때 쓰면 됨.

---

## 8. 트러블슈팅 — **반드시 한 번은 겪는 문제들**

### 8.1 "ESP32 가 포트에 안 잡혀"

| 증상 | 원인 / 해결 |
|------|-------------|
| Arduino IDE 의 Port 메뉴에 ESP32 안 보임 | 1) USB 케이블이 **데이터 케이블** 인지 확인 (충전 전용 케이블 흔함). 2) Windows 라면 [Espressif USB 드라이버](https://www.espressif.com/en/support/download/other-tools) 설치. 3) 다른 USB 포트 시도. 4) USB-C 케이블 양쪽 뒤집어서 다시 꽂아봐. |
| 포트는 잡혔는데 업로드 실패 | ESP32-S3 보드의 **BOOT 버튼** 누른 채로 **RESET 버튼** 짧게 누른 뒤 BOOT 떼기 → 부트로더 모드. 그 후 업로드. |
| "A fatal error occurred: Failed to connect" | 위 부트로더 모드 진입. 또는 Arduino IDE 의 **Upload Speed** 를 `460800` 으로 낮춰. |

### 8.2 "프레임이 다 0 으로만 나옴"

| 증상 | 원인 / 해결 |
|------|-------------|
| 모든 셀의 ADC 값이 0 | 깔판이 MUX 와 연결 안 됐거나, ESP32 의 ADC 핀이 잘못 꽂힘. GPIO 1 (ADC1_CH0) 인지 확인. 점퍼와이어 다 뽑고 처음부터. |
| 누르지 않아도 값이 4095 (최대치) | 풀-다운 저항 빠짐. 열 MUX SIG 단자에 GND 로 10 kΩ 풀-다운 걸어. |
| 한 행만 0 | 그 행의 MUX 채널 (Y0~Y15) 가 깔판 가로줄에 연결 안 됨. |

### 8.3 "Cloud 가 inspect 에서 500 에러 토함"

```
HTTP 500: Internal Server Error
```

`docker compose logs api` 로 실제 에러 메시지 봐. 99% 는:
- 라인이 아직 calibrated 상태가 아님 → `/v1/lines/{id}/calibrate` 먼저 호출
- 프레임 크기가 line.rows × line.cols 와 안 맞음 → ESP32 가 16×16 보내는데 라인을 8×8 로 만들었거나
- `frame` 배열이 list 가 아니고 string 임 → JSON 직렬화 확인

### 8.4 "전부 다 작동했는데 verdict 가 항상 pass 만 나옴"

- 캘리브레이션 때 **다양한 정상품 5개** 를 줬어야 함. 같은 거 5번 흘리면 모델이 그것만 정상으로 학습.
- 임계값 (`threshold_score`) 이 너무 높음. `PATCH /v1/lines/{id}` 로 낮춰봐. 기본값 4.0 → 2.5 로.

### 8.5 "라미네이팅 후 깔판이 죽었음"

- 라미네이팅 머신 온도가 너무 높았음 (보통 110°C 가 적정, 140°C 넘으면 Velostat 손상).
- 라미네이팅 전에 멀티미터 검사 했어야 함.
- 새로 만들어. 1만원짜리 부품 또 만드는 거니까 슬프지만 학습비.

### 8.6 "발주한 부품이 통관에서 막혀"

- **개인통관고유부호 안 적었음** → 관세청 [unipass.customs.go.kr](https://unipass.customs.go.kr) 에서 발급 (1분, 무료).
- **합산 $150 초과** → 부가세 + 관세 부과됨. 결제하면 풀려.
- **샘플로 분류 안 된 통신 부품** → ESP32 같은 거 가끔 SIM/WiFi 모듈로 분류돼서 까다로워짐. KC 인증 면제 신청서가 필요할 수도 있음. 디바이스마트에서 사면 다 처리되어 있어서 편함.

---

## 9. 그래서 너 진짜 할 일 (체크리스트)

이거 그대로 따라가면 됨. 못 하겠으면 어디서 막혔는지 형한테 톡해.

### Week 1 — 발주

- [ ] [`HOW_IT_WORKS.md`](./HOW_IT_WORKS.md) 읽음 (30분)
- [ ] 이 문서 (`HARDWARE_BUILD_GUIDE.md`) 1~3절까지 읽음 (30분)
- [ ] Adafruit 계정 + 개인통관고유부호 발급 (10분)
- [ ] 디바이스마트 / 쿠팡 / Adafruit 결제 (30분)
- [ ] 영수증/세금계산서 챙김 (자동)
- [ ] 형한테 발주 완료 + 예상 도착일 톡 (1분)

### Week 2 — 조립 + 펌웨어

- [ ] 부품 박스 다 까서 잃어버리기 전에 한 곳에 모음 (15분)
- [ ] 깔판 만들기 (1시간)
- [ ] 깔판 멀티미터 검사 통과 (10분)
- [ ] 라미네이팅 (30분)
- [ ] 스캐너 보드 (브레드보드 + 점퍼) (1시간)
- [ ] ESP32 펌웨어 굽기 (30분)
- [ ] `read_frames.py` 로 프레임 나오는 거 확인 (10분)
- [ ] 형한테 깔판 누르면서 ADC 값 올라가는 영상 보냄 (5분, 자랑하라)

### Week 3 — 백엔드 연결 + 첫 inspect

- [ ] Docker Desktop 설치 (15분)
- [ ] `docker compose up` 으로 백엔드 띄움 (10분)
- [ ] `gateway.py` 돌려서 라인 생성 → 5 프레임 → calibrate → inspect 까지 한 번에 진행 (30분)
- [ ] 정상품 / 비정상품 구분되는 거 영상 찍음 (10분)
- [ ] 형한테 영상 + "**진짜 됨**" 톡 (1분) ⭐

이 시점에서 형이랑 너 둘 다 **데모할 수 있는 상태**. 영업 미팅 잡자.

---

## 10. 마지막 — 격려와 약 올림

> 너 이거 만들면 진심으로 한국에서 이런 거 만든 사람 30명도 안 됨. 셋 다 학회 논문 쓴 사람들이고 양산 못 시킴. 우리는 양산 + SaaS 까지 갈 거고, 거기까지 가는 첫 발이 너 손에서 만들어진 깔판 한 장이야.
>
> 막히면 형한테 톡해. 단 "안 됨" 만 보내지 마. 사진 + 어디까지 했는지 + 어디서 막혔는지 같이 보내. 그래야 빠르게 답함.
>
> 인두에 손 대지 마라. 진짜로. 한 번 데면 1주일 일 못 함.
>
> 가자. 영업도 영업이지만 일단 우리 손으로 1대 만들어보자. 일단 만들어. 그 뒤는 다 쉬워.
>
> — 형
